[build]
  command = "bun run build"
  publish = ".svelte-kit/netlify"

[build.environment]
  NODE_VERSION = "20"

# Redirect all requests to SvelteKit
[[redirects]]
  from = "/*"
  to = "/.netlify/functions/render"
  status = 200

