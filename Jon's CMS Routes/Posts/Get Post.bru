meta {
  name: Get Post
  type: http
  seq: 2
}

get {
  url: {{host}}/api/v1/posts/{{postId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{api_key}}
}

settings {
  encodeUrl: true
  timeout: 0
}
